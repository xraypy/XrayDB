name: xraydb python, macos, conda

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-latest
    strategy:
      max-parallel: 5
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python from Miniconda
      uses: conda-incubator/setup-miniconda@v2.2.0
      with:
        miniconda-version: latest
        channels: conda-forge
        channel-priority: strict
        activate-environment: anaconda-client-env
        python-version: ${{ matrix.python-version }}
    - name: Install conda dependencies from conda-forge
      run: |
        which conda
        conda init
        conda info
        conda list
        conda activate anaconda-client-env
        conda install -y -c conda-forge "numpy>=2.0" "scipy>=1.15" pytest pytest-cov coverage setuptools pip
        conda list
    - name: Install xraydb with pip and run tests
      run: |
        which conda
        which python
        cd python
        pip install ".[all]"
        cd tests
        python -m pytest
